'use strict';

// eslint-disable-next-line no-extra-parens, no-empty-function
const cached = /** @type {import('async-function').AsyncFunctionConstructor} */ (async function () {}.constructor);

/** @type {import('async-function')} */
module.exports = () => cached;

